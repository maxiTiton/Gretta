/**
 * Productos Service
 * Servicios para gestión de productos
 * 
 * TODO: Implementar las siguientes funciones:
 * - getProductos(filters): obtener lista de productos con filtros opcionales
 * - getProductoById(id): obtener producto por ID
 * - getProductosByCategoria(categoriaId): filtrar por categoría
 * - searchProductos(query): buscar productos
 * - createProducto(data): crear nuevo producto (admin)
 * - updateProducto(id, data): actualizar producto (admin)
 * - deleteProducto(id): eliminar producto (admin)
 * - Todas las funciones deben usar el cliente de Supabase
 */

import { supabase } from './supabase'

// TODO: Implementar funciones de productos
