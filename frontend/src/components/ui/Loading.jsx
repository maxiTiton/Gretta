/**
 * Loading Component
 * Indicador de carga reutilizable
 * 
 * TODO:
 * - Props: size (sm, md, lg), text, fullScreen
 * - Spinner animado con colores de marca
 * - Variante fullscreen para loading de página
 */

export default function Loading({ size = 'md', text }) {
  return (
    <div>
      {/* TODO: Implementar spinner */}
    </div>
  )
}
