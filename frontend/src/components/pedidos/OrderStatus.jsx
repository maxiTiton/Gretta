/**
 * OrderStatus Component
 * Badge de estado de pedido
 * 
 * TODO:
 * - Props: estado
 * - Colores según estado (pendiente, confirmado, preparando, listo, entregado, cancelado)
 * - Iconos representativos
 * - Usar Badge component
 */

export default function OrderStatus({ estado }) {
  return (
    <span>
      {/* TODO: Implementar badge de estado */}
    </span>
  )
}
