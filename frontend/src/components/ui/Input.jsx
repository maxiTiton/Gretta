/**
 * Input Component
 * Campo de entrada reutilizable con label y error
 * 
 * TODO:
 * - Props: label, error, type, placeholder
 * - Integración con React Hook Form
 * - Variantes: text, email, password, number, textarea
 * - Mostrar mensajes de error con estilo
 */

export default function Input({ label, error, ...props }) {
  return (
    <div>
      {/* TODO: Implementar input con label y error */}
    </div>
  )
}
